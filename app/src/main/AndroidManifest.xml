<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".NewRestaurantActivity"
            android:label="Enter Restaurant Info"
            />
        <activity
            android:name=".RestaurantChooserActivity"
            android:label="@string/restaurant_chooser" />
        <activity
            android:name=".SplitBillActivity"
            android:label="@string/split_bill" />
        <activity
            android:name=".TipResultActivity"
            android:label="@string/tip_result" />
        <activity
            android:name=".ExperienceActivity"
            android:label="@string/add_experience" />
        <activity
            android:name=".ViewHistoryActivity"
            android:label="@string/ViewHistory_activity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/main_acitivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
    </application>

</manifest>